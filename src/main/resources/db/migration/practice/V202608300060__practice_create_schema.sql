-- Module practice — Attempt runtime, grading, topic result (ADR-004)
-- Một module sở hữu đúng một schema (§8). Không module nào ghi vào schema của module khác.
CREATE SCHEMA IF NOT EXISTS practice;
